interface CreepMemory {
    role: string;
}

interface PowerCreepMemory {
    room: string;
}

interface Memory {
    logLevel: LogLevel;
    age: number;
    genPixel: boolean;

    version: number;
}

interface RoomMemory {
    version: number;
}
